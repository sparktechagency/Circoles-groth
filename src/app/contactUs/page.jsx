import ContactForm from '@/components/ContactForm';
import React from 'react';

const page = () => {
    return (
        <div>
            <ContactForm/>
        </div>
    );
};

export default page;